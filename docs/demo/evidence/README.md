# Visual evidence

The JPEG files in this directory are generated by
`frontend/tests/demo.spec.ts` from the deterministic local application. They
are intentionally viewport captures rather than full-page recordings so the
review set stays small and durable.

Regenerate them with:

```powershell
Set-Location frontend
npm run test:browser
```
